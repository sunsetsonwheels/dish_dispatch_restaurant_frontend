name: dish_dispatch_restaurant_frontend
description: "A new Flutter project."
publish_to: "none"
version: 0.1.0

environment:
  sdk: ">=3.4.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  json_annotation: ^4.9.0
  http: ^1.2.1
  cached_network_image: ^3.3.1
  provider: ^6.1.2
  intl: ^0.19.0
  flutter_secure_storage: ^4.2.1
  routemaster: ^1.0.1
  shared_preferences: ^2.2.3
  image_picker: ^1.1.1
  animated_rating_stars: ^1.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
